# 2026-06-09 工作日志

## 补同步4个项目文件到 GitHub
- `PROJECT_BRIEF.md` — 项目交接文档
- `courseware-general-future-tense.md` — 课件内容素材
- `generate-ppt.cjs` — PPT 生成脚本
- `General-Future-Tense-30min-Experience.pptx` — 参考模板
- commit: `17891b4`，已推送

## 服务器连接问题排查
- 网站无法访问 → 排查发现 PM2/Nginx 均正常运行（200 OK）
- 根因：用户通过 `:3001` 端口访问，防火墙未开放 3001 端口
- 解决：80 端口已开放，引导用户用 `http://82.156.239.239`（不带端口号）访问

## 服务器配置速查
- IP: 82.156.239.239
- 访问方式: http://82.156.239.239（Nginx 80 → 转发到 Node 3001）
- SSH: `ssh edu-ai-server`
- 部署: `git push` → `ssh edu-ai-server "cd ~/edu-ai-teacher && git pull && npm run build && pm2 restart edu-ai-teacher"`

## 建立自动同步机制 (10:46)
### 用户级规则
- `~/.workbuddy/MEMORY.md` — 写入跨项目通用规则：验收后自动同步流程
- 规则触发条件：用户表示"可以了""没问题""通过"等验收信号

### 项目文档 Git 同步方案
- 创建 `docs/memory/` 目录（项目内），纳入 Git 版本管理
- 记忆文件双写：`.workbuddy/memory/`（WorkBuddy会话） + `docs/memory/`（Git同步）
- `docs/README.md` — 项目文档目录说明
- `docs/memory/MEMORY.md` — 项目规范和服务器信息

### 创建 auto-git-sync Skill
- 位置：`~/.workbuddy/skills/auto-git-sync/SKILL.md`
- 用途：验收后自动执行 记忆更新 → docs同步 → git提交推送 → 服务器部署
- commit: `684820c`

### 近义表达处理
- 精确触发词（自动执行）：可以了、OK、没问题、通过、确认、好的、行了、搞定、就这样、完成
- 近义表达（先确认）：挺好的、还行、差不多了、看着不错、先这样吧

## PPT生成器重构 — 模板风格 (11:00)

### 模板分析
- 参考模板：`数学冲刺学习计划.pptx`（12页，方正准圆字体，16:9）
- 色彩：标题 #1F2937，强调 #F97316（橙），正文 #4B5563
- 版式：标题+副标题区 + 三栏卡片布局，顶部色条装饰

### 实现方案
- `server/generate_ppt.py` — python-pptx 生成器，从零构建9页模板风格PPT
- 9页：封面 → 诊断概览 → 薄弱知识点 → 三阶段总览 → 3×阶段详情 → 学习安排 → 结尾
- 每页带演讲者备注
- 卡片设计：白色背景+色条顶装饰，对应模板卡片风格

### 服务器集成
- `server/index.js`：新增 `/api/generate-ppt` 端点
- 接收 JSON 诊断数据 → spawn python3 → 返回 .pptx 文件下载
- 修复了重复 `existsSync` import 导致的启动崩溃

### 前端对接
- `src/views/Courseware.vue`：downloadPPT 改为 fetch 调用 `/api/generate-ppt`
- downloadPDF 重写：生成模板风格 HTML（5页：封面/诊断/薄弱点/三阶段+例题/结尾）
- 移除前端 pptxgenjs 依赖

### 服务器部署
- 服务器安装 python-pptx（pip3 install python-pptx）
- 测试通过：9页PPT生成 + 200状态码
- commit: `3cd303a`（feat）+ `11d6cc6`（fix duplicate import）
