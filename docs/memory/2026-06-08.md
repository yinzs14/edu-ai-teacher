# 2026-06-08 工作日志

## 项目审查：edu-ai-teacher

- 路径：`D:\AI备课助手\edu-ai-teacher`
- Git：https://github.com/yinzs14/edu-ai-teacher
- 线上：https://preeminent-maamoul-90e7ba.netlify.app

### 项目概况
面向中小学教师的 AI 备课助手，Vue 3 + Vite + Netlify Functions，部署于 Netlify。

### 已完成
- 学情诊断（Diagnose.vue）：多图上传、框选截图、OCR（百度+Qwen-VL）、DeepSeek分析、雷达图，完成度 ~90%
- 课件预览/在线编辑/TXT下载（Courseware.vue），完成度 ~45%（缺 PPT 导出）
- 知识树折叠展开 UI（KnowledgeTree.vue），完成度 ~50%（缺真实教材数据+示例题）

### 待开发（高优先级）
1. 课件 .pptx 导出（推荐 pptxgenjs 前端库）
2. 知识树真实数据 + 每条知识点配示例题
3. 响应式适配（手机/平板）

### OCR 演进历史
v1 百度 → v2 accurate_basic/general_basic → v3 阿里云 Qwen-VL-OCR（最新）

---

## Agent.md 文档更新

- 补充了学情诊断双输出方案（section 2.1）：
  1. 家长沟通话术文档：用于老师与家长语音沟通，结构化呈现学情
  2. 定制学习方案（PPT+PDF）：12页模板结构，PPT 带备注讲法用于上课，PDF 用于家长
- 更新了待完成任务清单，细化为新需求对应的具体开发任务
- 添加了参考模板分析（数学冲刺学习计划.pptx 的 12 页结构）
- 本地 commit: `0c4235b`，已成功 push
- 配置了 SSH 密钥 + ssh.github.com:443（绕过防火墙），实现免密自动推送

---

## 家长沟通话术功能开发

- 更新 `api/diagnose.js` 的 SYSTEM_PROMPT，新增 communicationScript 五段式输出（stageKnowledge/mastered/weaknesses/solutions/talkingTips）
- 更新 `Diagnose.vue`：新增话术卡片展示 + 一键复制按钮，新增 ChatDotRound/CopyDocument 图标
- commit: `e4cce40`

## 诊断→课件数据流 + PPT/PDF 双输出

- Diagnose.vue 的 goCourseware() 将完整诊断数据存入 localStorage
- Courseware.vue 重构：从 localStorage 读取诊断数据，动态生成课件内容（按薄弱点排序）
- 新建 `src/utils/generatePPT.js`：pptxgenjs 生成 6 页 PPT（封面/诊断/薄弱点/三阶段计划/课表/寄语），每页带备注讲法
- PDF 导出：生成格式化 HTML → 浏览器打印为 PDF
- commit: `f00fdd8`, `bf57e22`

## 国内部署方案设计与基础设施搭建

- Netlify 额度用完，推荐腾讯云轻量应用服务器（2核2G，¥50/月起）
- 创建 `server/index.js`：Express 后端，包裹原 Netlify Functions 三个 API（ocr / vision-ocr / diagnose），统一 CORS + JSON 解析
- 创建 `server/package.json`：最小依赖（express + cors + dotenv）
- 创建 `.env.example`：API 密钥模板
- 创建 `deploy.sh`：一键部署脚本（安装 Node/Nginx/PM2 → 构建前端 → 安装后端依赖 → 启动 PM2 → 配置 Nginx）
- 创建 `ecosystem.config.js`：PM2 配置（自动重启 + 日志）
- 创建 `nginx.conf`：Nginx 配置（静态文件 + API 代理 + SPA fallback + Gzip）
- 更新 `vite.config.js`：添加 `/api` 代理到 localhost:3001（本地开发用）
- 更新 `package.json`：新增 `server`/`start` 脚本，pptxgenjs 从 devDeps 移到 deps
- 更新 `.gitignore`：新增 logs/
- 更新 `Agent.md`：Section 11 完整部署指南 + 技术栈标注
- commit: `ef7aef9`

### 部署流程
用户只需：①买服务器 ②SSH 登录 ③ git clone + bash deploy.sh
后续更新：git pull + bash deploy.sh

## 服务器部署上线

- **服务器 IP**: 82.156.239.239
- **SSH 别名**: `edu-ai-server`（本地 `~/.ssh/config` 已配置，RSA 密钥免密）
- **状态**: 已部署运行，前端 200 + API 200
- **环境变量**: 已配置 DASHSCOPE_API_KEY、DEEPSEEK_API_KEY、BAIDU_API_KEY、BAIDU_SECRET_KEY、PORT=3001
- **修复**: 目录权限问题 → `sudo chmod 755 dist/` + 父目录权限
- **GitHub SSH**: 服务器生成 ed25519 密钥，走 ssh.github.com:443 绕墙，添加为 Deploy Key
- **后续更新**: 本地 `git push` → `ssh edu-ai-server "cd ~/edu-ai-teacher && git pull && npm install && npm run build && cd server && npm install && cd .. && pm2 restart edu-ai-teacher"`

---

## Diagnose.vue 全面重构 (15:35)

### 移除
- 学情雷达图（数据仍保留在 localStorage 供 PPT/PDF 使用）
- OCR识别文字编辑区（隐藏OCR结果，后台自动识别）

### 新增
- 教师补充题目输入框（打字/粘贴，替代OCR编辑区）
- 全部图片框选汇总显示（不随切图消失）
- 分析后按钮变「🔄重新分析」
- 分析后自动滚动到结果区
- 左侧操作提示（未分析时）

### 修复
- 多图框选累计 → 切换图片不清除选中，汇总显示所有图的框选
- 识别+分析一体化 → 点击「分析错题」自动后台OCR所有图片→调用AI诊断
- 清空全部框选同步重置分析状态
- 新增图片时同步重置分析状态

### 重构布局
- 左栏 350px：上传/缩略图/预览/框选汇总/补充/分析按钮
- 右栏自适应：沟通话术↗(最重要) → 学习方案 → 薄弱知识点 → 生成课件
- 折叠设计：3个section可折叠，话术5个子块独立折叠
- 默认展开：话术中「有待提升」「解决建议」默认展开 → 首次家长沟通最关心
- 学习方案第一阶段默认展开

### 构建验证通过
- Diagnose.js: 19.97 kB (gzip: 7.85 kB)
- Diagnose.css: 10.26 kB (gzip: 2.25 kB)

---

## 师生姓名 + 话术口语化 + 左栏收窄 (15:55)

### 备份
- `src/views/Diagnose.vue.backup` — 回滚用

### 新增功能
1. **师生姓名输入框** — 左上角醒目位置，带说明文字（老师花名用于PPT/PDF品牌标识，学生姓名用于个性化话术）
2. **分析后左栏自动收窄+变灰** — 350px→280px, filter: grayscale(0.3), hover恢复, 预览图隐藏, 缩略图缩小
3. **API 口语化升级** — SYSTEM_PROMPT 重写，强调口语短句、生活化比喻、自然使用学生名字、禁止清单式写法

### 修改文件
- `server/index.js`: SYSTEM_PROMPT 重写 + 接收 studentName/teacherName
- `api/diagnose.js`: 同上（Netlify版）
- `src/views/Diagnose.vue`: 姓名输入框/左栏动效/API传参/复制话术带姓名
- `src/views/Courseware.vue`: PPT数据从diagnosisData读取姓名

---

## 姓名区折叠 + 预览调大 + 右侧折叠 + 新手引导 (17:35)

### 3个UI优化
1. 姓名区填完折叠 → 花名和学生姓名都填好后显示「确认✓」按钮，点击后收为一行 `🧑‍🏫 张老师 | 👦 小明 [编辑]`，释放~80px空间给预览图
2. 预览图调大 → max-height 160→220px，底部增加永久可见蓝色引导条「👆 点击图片开始框选错题区域」，首次无框选时右侧红色脉冲箭头提示
3. 右侧默认全部折叠 → 沟通话术/学习方案/薄弱知识点 3个section默认折叠，每块显示一行预览文字。点标题展开查看详情。一屏内能看到所有板块

### 部署
- 构建验证通过 → git push → 服务器部署成功
